# 🚀 Firebase 배포 가이드

> 입고요청서 관리시스템 v4.1 Firebase Hosting 배포 및 업데이트 가이드

## 📋 개요

이 문서는 입고요청서 관리시스템을 Firebase Hosting에 배포하고, 향후 업데이트를 쉽게 배포하는 방법을 안내합니다.

---

## 🎯 현재 배포 정보

### 🌐 **웹사이트 접속 URL**
- **Primary URL**: https://inventory-management-212d7.web.app
- **Alternative URL**: https://inventory-management-212d7.firebaseapp.com

### 🔐 **관리자 접속 정보**
- **관리자 ID**: `admin`
- **관리자 패스워드**: `admin` ⚠️ *프로덕션 환경에서는 반드시 변경하세요*
- **관리자 모드**: 웹사이트 우측 상단 🔐 관리자 모드 버튼 클릭

### 📊 **Firebase Console**
- **프로젝트 관리**: https://console.firebase.google.com/project/inventory-management-212d7/overview

---

## ⚡ 빠른 업데이트 배포

### 1️⃣ **원스텝 배포 (추천)**
```bash
# 프로젝트 디렉토리에서 실행
./deploy.sh
```

### 2️⃣ **수동 배포**
```bash
# 프로젝트 디렉토리로 이동
cd "/Users/jinseunghyun/inventory project"

# 최신 파일을 public 디렉토리에 복사
cp index.html public/

# Firebase에 배포
firebase deploy
```

### 3️⃣ **빠른 Hosting만 배포**
```bash
# Hosting만 배포 (더 빠름, 약 10-20초)
firebase deploy --only hosting
```

---

## 🔧 배포 스크립트 사용법

### **deploy.sh 스크립트**
프로젝트 루트에 있는 `deploy.sh` 스크립트를 사용하면 한 번에 배포가 완료됩니다.

```bash
#!/bin/bash
# 입고요청서 관리시스템 Firebase 배포 스크립트

echo "🚀 Firebase 배포를 시작합니다..."
cd "$(dirname "$0")"
cp index.html public/
firebase deploy
echo "✅ 배포가 완료되었습니다!"
echo "🌐 사이트 URL: https://inventory-management-212d7.web.app"
```

### **스크립트 실행 권한 설정**
```bash
chmod +x deploy.sh
```

---

## 📝 배포 워크플로우

### **일반적인 업데이트 과정**

1. **코드 수정**
   - `index.html` 파일 수정
   - 기능 추가, 버그 수정 등

2. **로컬 테스트** (선택사항)
   ```bash
   # 로컬 서버로 테스트
   python3 -m http.server 8000
   # 또는
   npx http-server -p 8000
   ```

3. **배포 실행**
   ```bash
   ./deploy.sh
   ```

4. **배포 확인**
   - 웹사이트 접속하여 업데이트 확인
   - 캐시 새로고침: `Ctrl+F5` (Windows) 또는 `Cmd+Shift+R` (Mac)

### **배포 시간**
- **전체 배포**: 약 30초 - 1분
- **Hosting만 배포**: 약 10-20초
- **초기 설정**: 이미 완료됨 ✅

---

## 🛠️ 고급 배포 옵션

### **서비스별 개별 배포**

```bash
# Hosting만 배포
firebase deploy --only hosting

# Firestore 규칙만 배포
firebase deploy --only firestore:rules

# Firestore 인덱스만 배포
firebase deploy --only firestore:indexes

# 모든 서비스 배포
firebase deploy
```

### **배포 확인 및 모니터링**

```bash
# 배포 상태 확인
firebase hosting:releases

# 프로젝트 정보 확인
firebase projects:list

# 현재 사용 중인 프로젝트 확인
firebase use
```

---

## 🔍 배포 후 확인사항

### ✅ **필수 체크리스트**

1. **웹사이트 접속**
   - [ ] https://inventory-management-212d7.web.app 접속 확인
   - [ ] 페이지 로딩 정상 확인

2. **버전 확인**
   - [ ] 페이지에서 "v4.1" 버전 표시 확인
   - [ ] Firebase 연결 상태 표시줄 초록색 확인

3. **관리자 기능 테스트**
   - [ ] 🔐 관리자 모드 버튼 클릭
   - [ ] ID: `admin`, Password: `admin`으로 로그인
   - [ ] 관리자 메뉴 정상 표시 확인

4. **주요 기능 테스트**
   - [ ] 보관품 데이터 로딩 확인
   - [ ] 입고요청서 작성 기능 확인
   - [ ] 생산시설 관리 기능 확인 (관리자 모드)
   - [ ] 품목 관리 기능 확인 (관리자 모드)

---

## 🚨 문제 해결

### **캐시 문제**
업데이트가 즉시 반영되지 않을 때:

```bash
# 강제 캐시 무효화로 배포
firebase deploy --only hosting --force

# 브라우저 캐시 강제 새로고침
# Windows: Ctrl+F5
# Mac: Cmd+Shift+R
```

### **배포 실패 시**

1. **Firebase CLI 버전 확인**
   ```bash
   firebase --version
   # 최신 버전으로 업데이트
   npm install -g firebase-tools
   ```

2. **로그인 상태 확인**
   ```bash
   firebase login --reauth
   ```

3. **프로젝트 연결 확인**
   ```bash
   firebase use inventory-management-212d7
   ```

### **일반적인 오류 및 해결방법**

| 오류 메시지 | 해결방법 |
|------------|----------|
| `Error: No project active` | `firebase use inventory-management-212d7` 실행 |
| `Error: Permission denied` | `firebase login --reauth` 실행 |
| `Build failed` | `index.html` 파일 문법 오류 확인 |
| `Upload failed` | 네트워크 연결 확인 후 재시도 |

---

## 🔒 보안 및 관리

### **관리자 비밀번호 변경**

⚠️ **중요**: 프로덕션 환경에서는 반드시 관리자 비밀번호를 변경하세요.

`index.html` 파일에서 다음 부분 수정:
```javascript
// 약 1825번째 줄 근처
if (adminId === 'your-new-id' && adminPassword === 'your-secure-password') {
```

### **Firebase 프로젝트 보안**

1. **Firestore 보안 규칙**: `firestore.rules` 파일로 관리
2. **호스팅 보안 헤더**: `firebase.json` 파일에서 설정
3. **프로젝트 권한 관리**: Firebase Console에서 관리

---

## 📊 배포 모니터링

### **배포 히스토리 확인**
```bash
# 최근 배포 목록 확인
firebase hosting:releases

# 특정 버전으로 롤백 (Firebase Console 권장)
```

### **사용 통계 확인**
- Firebase Console → Analytics 에서 사용 통계 확인
- Performance Monitoring으로 성능 모니터링

---

## 🎯 프로덕션 배포 체크리스트

### **배포 전 확인사항**
- [ ] 로컬에서 모든 기능 테스트 완료
- [ ] 관리자 비밀번호 변경 (필요시)
- [ ] Firebase 프로젝트 권한 확인
- [ ] 백업 완료

### **배포 실행**
- [ ] `./deploy.sh` 실행
- [ ] 배포 성공 메시지 확인
- [ ] 웹사이트 접속 테스트

### **배포 후 확인**
- [ ] 모든 기능 정상 동작 확인
- [ ] 관리자 모드 접속 확인
- [ ] 데이터 동기화 확인
- [ ] 사용자에게 업데이트 공지

---

## 📞 지원 및 문의

### **기술 지원**
- **GitHub Repository**: 프로젝트 저장소 Issues 탭
- **Firebase Console**: https://console.firebase.google.com/project/inventory-management-212d7
- **기술 문서**: `TECHNICAL.md` 참조

### **자주 묻는 질문**

**Q: 배포 후 변경사항이 보이지 않아요**
A: 브라우저 캐시를 강제로 새로고침하세요 (Ctrl+F5 또는 Cmd+Shift+R)

**Q: 관리자 로그인이 안 돼요**
A: ID: `admin`, Password: `admin`을 정확히 입력했는지 확인하세요

**Q: Firebase 비용이 궁금해요**
A: 현재 설정으로는 무료 한도 내에서 사용 가능합니다. Firebase Console에서 사용량 확인 가능합니다.

---

## 🔄 자동화 고도화

### **GitHub Actions 자동 배포** (선택사항)

`.github/workflows/firebase-deploy.yml` 파일을 생성하여 GitHub에 푸시할 때마다 자동 배포 가능:

```yaml
name: Firebase Deploy
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v2
    - run: npm install -g firebase-tools
    - run: cp index.html public/
    - run: firebase deploy
      env:
        FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
```

---

**📝 문서 마지막 업데이트**: 2024년 12월  
**🔖 문서 버전**: v1.0  
**🚀 시스템 버전**: v4.1  
**👥 작성자**: 개발팀

---

**🎉 축하합니다! 이제 언제든지 쉽게 업데이트하고 배포할 수 있습니다!**